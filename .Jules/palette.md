## 2024-05-23 - [Chat Accessibility]
**Learning:** Icon-only buttons and generic inputs (like message fields) are often missed in accessibility passes. Adding explicit `aria-label` attributes is a quick, high-impact fix for screen reader users.
**Action:** Always verify icon-only buttons have an `aria-label` or `sr-only` text description.
